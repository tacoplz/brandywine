<br><center><b><p id="notice"><%= notice %></p></b></center>
<div class="container">
  <%#= the location for the jumbotron photo is /app/assets/images/jumbotron-bg
  the link is mentioned in the code in bootstrap.css %>
  <center><div class="jumbotron">
    <stlye>
    <h1>Brandywine Furniture</h1>
    <font color= white><h2>Quality at a Comfortable Price<h2></font>
    </style>
  </div></center>
</div>
<center><table>

  <tbody>
    <% @homepage_posts.each do |homepage_post| %>
      <tr>
        <td><h3><%= link_to homepage_post.post_title, homepage_post %></h3></td>
        <td><h5><%= homepage_post.created_at.strftime("%B %d, %Y") %></h5></td>
      </tr>
      <tr>
        <td><%= homepage_post.post_text %></td>
        <td><%= homepage_post.post_img_url %></td>
      </tr>
      <tr>
        <% if session[:user_role_id] %>
          <td><%= link_to 'Edit', edit_homepage_post_path(homepage_post), class: 'btn btn-secondary active' %></td>
          <td><%= link_to 'Delete', homepage_post, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-secondary active' %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table></center>

<br>
<% if session[:user_role_id] %>
  <%= link_to 'New Announcement', new_homepage_post_path, class: 'btn btn-secondary active' %>
<% end %>
</div>
