<div class="container">
<%= form_for(@furniture_room) do |f| %>
  <% if @furniture_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@furniture_room.errors.count, "error") %> prohibited this furniture_room from being saved:</h2>

      <ul>
      <% @furniture_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :room_image %><br>
    <%= f.file_field :room_image %>
  </div>

  <div class="field">
    <%= f.label :room_type %><br>
    <%= f.text_field :room_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
