<%= form_for(@about_u) do |f| %>
  <% if @about_u.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about_u.errors.count, "error") %> prohibited this about_u from being saved:</h2>

      <ul>
      <% @about_u.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :about_us_image %><br>
    <%= f.file_field :about_us_image %>
  </div>
  <div class="field">
    <%= f.label :phone_number %><br>
    <%= f.telephone_field(:phone_number) %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_area :email %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </div>
  <div class="field">
    <%= f.label :hours %><br>
    <%= f.text_area :hours %>
  </div>

  <div class="actions">
    <%= f.submit "Create About Us"%>
  </div>

<% end %>
